# Dépendances directes épinglées (==) pour une build Docker reproductible.
# Auditées avec `pip-audit` le 2026-08-28 : aucune vulnérabilité connue.
#
# Mise à jour : bumper une version ci-dessous, relancer `pip-audit` sur
# l'environnement résultant, retester l'app, puis committer. Ne PAS remonter
# `anthropic` en 1.x sans une migration dédiée (changement de version majeure
# — l'usage dans app/services/claude_service.py doit être revalidé).
#
# Seules les dépendances *directes* sont figées ici ; les transitives (starlette,
# httpx, anyio, cffi...) restent résolues au moment de la build. Pour un lock
# complet, passer à un requirements.txt compilé via pip-compile — non fait.
fastapi==0.141.1
uvicorn[standard]==0.52.1
anthropic==0.120.2
python-multipart==0.0.32
python-dotenv==1.2.2
pydantic==2.13.4
Pillow==12.3.0
pymupdf==1.28.0
weasyprint==69.0
